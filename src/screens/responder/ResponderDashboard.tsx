import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ResponderDashboard = () => {
  return (
    <View>
      <Text>ResponderDashboard</Text>
    </View>
  );
};

export default ResponderDashboard;

const styles = StyleSheet.create({});
