export interface Translation {
  greeting: string;
  welcome: string;
  home: string;
  settings: string;
  language: string;
  selectLanguage: string;
  logout: string;
  profile: string;
  messages: {
    success: string;
    error: string;
    loading: string;
  };
}